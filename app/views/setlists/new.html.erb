<% content_for :title, "New setlist" %>

<div class="mx-auto max-w-3xl">
  <div class="mb-4">
    <h1 class="text-2xl font-semibold text-slate-900">New setlist</h1>
    <p class="mt-1 text-sm text-slate-600">Start a new running order for a band.</p>
  </div>

  <div class="rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
    <%= render "form", setlist: @setlist %>
  </div>
</div>
