<% content_for :title, "New Band" %>

<div class="mx-auto max-w-2xl rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
  <h1 class="text-xl font-semibold text-slate-900">Create a band</h1>
  <p class="mt-2 text-sm text-slate-600">Add the basics now. You can fill in more details later.</p>

  <div class="mt-6">
    <%= render "form", band: @band %>
  </div>
</div>
