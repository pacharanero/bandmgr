<% content_for :title, "Edit Event" %>

<div class="mx-auto max-w-3xl rounded-lg border border-slate-200 bg-white p-6 shadow-sm">
  <h1 class="text-xl font-semibold text-slate-900">Edit event</h1>

  <div class="mt-6">
    <%= render "form", event: @event %>
  </div>
</div>
