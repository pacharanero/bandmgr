<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Bandmgr" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <style>
      body { font-family: "Helvetica Neue", Arial, sans-serif; color: #0f172a; margin: 32px; }
      h1 { font-size: 24px; margin-bottom: 4px; }
      h2 { font-size: 14px; text-transform: uppercase; letter-spacing: 0.06em; color: #64748b; margin-top: 24px; }
      .meta { color: #475569; font-size: 12px; }
      .toolbar { display: flex; gap: 12px; margin-bottom: 20px; }
      .toolbar a { color: #0f172a; text-decoration: none; border: 1px solid #cbd5f5; padding: 6px 10px; border-radius: 6px; font-size: 12px; }
      .settings { border: 1px solid #e2e8f0; border-radius: 8px; padding: 12px; margin-bottom: 16px; }
      .settings label { margin-right: 12px; font-size: 12px; }
      .song { border-bottom: 1px solid #e2e8f0; padding: 10px 0; }
      .song:last-child { border-bottom: 0; }
      .song-title { font-weight: 600; }
      .song-meta { font-size: 12px; color: #475569; margin-top: 4px; }
      .song-notes { font-size: 12px; color: #334155; margin-top: 6px; white-space: pre-wrap; }
      @media print {
        .toolbar, .settings, .print-help { display: none; }
        body { margin: 16px; }
      }
    </style>
  </head>
  <body>
    <%= yield %>
  </body>
</html>
